# AI语法诊断功能说明

## 功能概述

AI语法诊断是MCNP6 AI Assistant的核心功能之一，用于智能检测MCNP6输入文件的语法错误，并在编辑器中高亮显示错误和警告位置。

## 主要特性

### 1. 智能语法检测
- 自动检测MCNP6输入文件的语法错误
- 支持多种卡片类型的语法检查：
  - 单元格卡片（Cell Cards）
  - 表面卡片（Surface Cards）
  - 材料卡片（Material Cards）
  - 源定义（SDEF）
  - 计数卡（Tally Cards）
  - 数据卡（Data Cards）
- 检查参数格式和分隔符
- 验证数值范围和单位

### 2. 精确的行号定位
- AI返回每个错误的确切行号
- 支持快速定位到错误位置
- 提供详细的错误描述

### 3. 视觉化错误标记
- **错误高亮**：红色背景（#5A1D1D）
- **警告标记**：黄色背景（#4A3D00）
- 清晰区分错误和警告
- 支持清除所有标记

### 4. 详细的诊断报告
- 诊断总结
- 每个错误的详细信息：
  - 行号
  - 错误类型
  - 错误描述
  - 修正建议
- 警告信息
- 统计信息（错误数、警告数）

## 使用方法

### 方式1：通过菜单栏
1. 点击菜单栏"工具" → "AI语法诊断"（或按F8键）
2. 等待AI分析完成
3. 查看诊断结果和编辑器中的高亮标记

### 方式2：通过左侧面板
1. 点击左侧控制面板的"语法诊断"按钮
2. 等待AI分析完成
3. 查看诊断结果和编辑器中的高亮标记

## 诊断流程

```
用户输入 → AI分析 → 返回JSON结果 → 解析错误信息 → 高亮显示错误行 → 显示诊断报告
```

## 返回的JSON格式

```json
{
  "has_errors": true/false,
  "errors": [
    {
      "line": 行号,
      "type": "错误类型",
      "message": "错误描述",
      "suggestion": "修正建议"
    }
  ],
  "warnings": [
    {
      "line": 行号,
      "type": "警告类型",
      "message": "警告描述"
    }
  ],
  "summary": "总体诊断总结"
}
```

## 错误类型示例

### 1. 单元格卡片错误
```
错误 1:
  行号: 5
  类型: 单元格卡片错误
  描述: 单元格编号重复
  建议: 检查单元格编号是否唯一
```

### 2. 表面卡片错误
```
错误 2:
  行号: 12
  类型: 表面卡片错误
  描述: 表面类型 'sx' 不存在
  建议: 使用正确的表面类型（如 so, px, cz 等）
```

### 3. 材料卡片错误
```
错误 3:
  行号: 18
  类型: 材料卡片错误
  描述: 材料编号 2 未定义
  建议: 先定义材料卡片再使用
```

### 4. 源定义错误
```
错误 4:
  行号: 25
  类型: 源定义错误
  描述: SDEF 缺少必需参数 'pos'
  建议: 添加源位置参数
```

## 编辑器高亮说明

### 错误标记（红色背景）
- 颜色：#5A1D1D（深红色）
- 适用：语法错误、致命错误
- 优先级：高于警告标记

### 警告标记（黄色背景）
- 颜色：#4A3D00（深黄色）
- 适用：潜在问题、建议改进
- 优先级：低于错误标记

### 清除标记
- 点击"语法诊断"时自动清除之前的标记
- 编辑器内容改变时不会自动清除
- 需要重新运行诊断来更新标记

## 技术实现

### AI分析模块（ai_analyzer.py）
```python
def diagnose_syntax(self, input_content: str) -> Dict:
    """
    诊断MCNP6输入文件的语法错误
    
    参数:
        input_content: MCNP6输入文件内容
    
    返回:
        Dict: 包含诊断结果的字典
    """
```

### 编辑器模块（editor.py）
```python
def mark_error_lines(self, errors):
    """
    在编辑器中标记错误行
    
    参数:
        errors: 错误列表，每个错误包含行号等信息
    """

def mark_warning_lines(self, warnings):
    """
    在编辑器中标记警告行
    
    参数:
        warnings: 警告列表
    """

def clear_error_marks(self):
    """
    清除所有错误和警告标记
    """
```

### 主窗口模块（main_window.py）
```python
def diagnose_syntax(self):
    """
    启动AI语法诊断
    """

def on_diagnose_finished(self, result):
    """
    处理AI诊断结果
    """
```

## 使用建议

### 1. 诊断时机
- **编写完成后**：完成输入文件编写后进行诊断
- **修改后**：每次重大修改后重新诊断
- **运行前**：运行MCNP6前进行诊断

### 2. 诊断顺序
1. 先进行AI语法诊断（快速检查）
2. 查看高亮的错误行
3. 根据AI建议修正错误
4. 重新诊断确认修正
5. 使用MCNP6验证（如果可用）

### 3. 错误处理
- **立即修正错误**：红色背景的行必须修正
- **评估警告**：黄色背景的行根据情况决定是否修正
- **参考AI建议**：AI提供的修正建议很有参考价值
- **查阅MCNP6手册**：对于复杂问题，查阅官方文档

### 4. 最佳实践
- 逐步构建：逐步添加卡片，每步都进行诊断
- 保存版本：保存多个版本，便于回退
- 记录修正：记录常见错误和修正方法
- 学习模式：通过诊断结果学习MCNP6语法

## 快捷键

- `F8`: AI语法诊断

## 与其他功能的区别

| 功能 | 用途 | 输出 | 高亮 |
|------|------|------|-------|
| AI语法诊断 | 检测语法错误 | 详细错误报告 | ✓ 是 |
| AI分析输入文件 | 全面分析 | 结构化分析报告 | ✗ 否 |
| 验证输入文件 | 基本验证 | 简单验证结果 | ✗ 否 |

## 常见问题

### Q: AI诊断能检测所有错误吗？
A: AI诊断能检测大多数常见语法错误，但可能无法检测所有MCNP6特定错误。建议结合MCNP6运行验证。

### Q: 诊断结果不准确怎么办？
A: 
1. 检查AI配置（温度建议设置为0.2-0.4）
2. 尝试重新诊断
3. 查阅MCNP6官方文档
4. 使用MCNP6实际运行验证

### Q: 如何清除编辑器中的高亮标记？
A: 重新运行"语法诊断"会自动清除之前的标记。或者修改文件内容后标记会自动更新。

### Q: 诊断速度慢怎么办？
A: 
1. 使用本地AI模式（更快）
2. 减少输入文件长度
3. 调整最大令牌数（减少到1000-1500）
4. 检查网络连接（在线模式）

### Q: 支持哪些MCNP6版本？
A: AI诊断基于MCNP6通用语法，支持MCNP6、MCNP5、MCNPX等版本。

## 后续改进计划

- [ ] 支持实时语法检查（输入时自动诊断）
- [ ] 添加错误修复建议的一键应用
- [ ] 支持错误历史记录
- [ ] 添加语法错误统计图表
- [ ] 支持自定义错误标记颜色
- [ ] 添加错误导出功能

## 示例

### 示例1：检测单元格卡片错误

**输入：**
```
c 测试输入文件
1 1 -1.0 -1 imp:p=1
1 2 -2.0 -2 imp:p=1
2 0 2 imp:p=0
1 so 10
```

**诊断结果：**
```
=== 诊断总结 ===
发现 1 个语法错误

错误 1:
  行号: 3
  类型: 单元格卡片错误
  描述: 单元格编号 1 重复定义
  建议: 使用唯一的单元格编号
```

**编辑器显示：**
- 第3行高亮显示（红色背景）

### 示例2：检测表面卡片错误

**输入：**
```
c 测试输入文件
1 1 -1.0 -1 imp:p=1
2 0 1 imp:p=0
1 sx 10
```

**诊断结果：**
```
=== 诊断总结 ===
发现 1 个语法错误

错误 1:
  行号: 5
  类型: 表面卡片错误
  描述: 表面类型 'sx' 不存在
  建议: 使用正确的表面类型（如 so, px, cz 等）
```

**编辑器显示：**
- 第5行高亮显示（红色背景）

## 技术支持

如有问题或建议，欢迎反馈！
