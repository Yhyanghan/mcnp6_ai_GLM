# AI一键执行功能说明

## 功能概述

AI一键执行功能允许用户在AI给出诊断结果、优化建议或补全建议后，通过简单的点击操作直接在编辑器中应用这些建议，大大提高工作效率。

## 主要功能

### 1. 跳转到错误行
- **适用场景**：AI语法诊断
- **功能**：快速跳转到第一个错误行
- **按钮**："跳转到行"（蓝色）
- **快捷操作**：点击按钮后自动切换到编辑器标签页并定位到错误行

### 2. 应用补全建议
- **适用场景**：AI补全输入文件
- **功能**：将AI补全的完整内容应用到编辑器
- **按钮**："应用建议"（绿色）
- **操作**：点击后用AI补全的内容替换编辑器内容

### 3. 应用优化建议
- **适用场景**：AI优化建议
- **功能**：将AI优化建议追加到编辑器
- **按钮**："应用建议"（绿色）
- **操作**：
  - 点击后弹出确认对话框
  - 选择"是"：将优化建议追加到编辑器末尾
  - 选择"否"：仅在AI分析结果中查看

### 4. 清除结果
- **适用场景**：所有AI功能
- **功能**：清除AI分析结果标签页的内容
- **按钮**："清除"（红色）
- **操作**：点击后清空AI分析结果标签页

## 界面说明

### AI分析结果标签页工具栏

```
┌─────────────────────────────────────────────────────────┐
│ [跳转到行] [应用建议]              [清除] │
├─────────────────────────────────────────────────────────┤
│                                                 │
│              AI分析结果显示区域                  │
│                                                 │
│                                                 │
└─────────────────────────────────────────────────────────┘
```

### 按钮状态

| 按钮 | 颜色 | 启用条件 | 功能 |
|------|--------|-----------|------|
| 跳转到行 | 蓝色 (#0E639C) | 有错误时启用 | 跳转到第一个错误行 |
| 应用建议 | 绿色 (#107C10) | 补全/优化时启用 | 应用AI建议到编辑器 |
| 清除 | 红色 (#C53030) | 始终启用 | 清除AI分析结果 |

### 按钮禁用状态

- **跳转到行**：当没有错误时禁用（灰色 #3E3E42）
- **应用建议**：
  - 诊断模式：禁用（诊断结果不直接应用）
  - 分析模式：禁用（分析结果仅供参考）
  - 补全模式：启用
  - 优化模式：启用

## 使用流程

### 语法诊断流程

```
1. 在编辑器中输入MCNP6代码
   ↓
2. 点击"语法诊断"按钮
   ↓
3. AI返回诊断结果
   ↓
4. 查看AI分析结果标签页
   ↓
5. 点击"跳转到行"按钮
   ↓
6. 自动跳转到编辑器并定位到错误行
   ↓
7. 根据AI建议修正错误
```

### 补全输入文件流程

```
1. 在编辑器中输入部分MCNP6代码
   ↓
2. 点击"补全输入文件"按钮
   ↓
3. AI返回补全建议
   ↓
4. 查看AI分析结果标签页
   ↓
5. 点击"应用建议"按钮
   ↓
6. 自动将补全内容应用到编辑器
   ↓
7. 编辑器显示完整的MCNP6代码
```

### 优化建议流程

```
1. 在编辑器中输入完整MCNP6代码
   ↓
2. 点击"优化建议"按钮
   ↓
3. AI返回优化建议
   ↓
4. 查看AI分析结果标签页
   ↓
5. 点击"应用建议"按钮
   ↓
6. 弹出确认对话框
   ↓
7. 选择"是"或"否"
   ↓
8. 如果选择"是"，优化建议追加到编辑器末尾
```

## 技术实现

### AIOutputPanel类

```python
class AIOutputPanel(QWidget):
    apply_suggestion = pyqtSignal()
    jump_to_line = pyqtSignal(int)
```

**信号**：
- `apply_suggestion`：应用建议信号
- `jump_to_line`：跳转到行信号（带行号参数）

**方法**：
- `set_content(content)`：设置AI分析结果内容
- `clear()`：清除AI分析结果
- `set_mode(mode)`：设置当前模式（diagnose/optimize/complete/analyze）
- `set_errors(errors)`：设置错误列表
- `on_jump_to_line()`：跳转到错误行
- `on_apply_suggestion()`：应用AI建议
- `on_clear()`：清除AI分析结果

### 主窗口方法

```python
def jump_to_editor_line(self, line_num):
    """跳转到编辑器指定行"""
    
def apply_ai_suggestion(self):
    """应用AI建议到编辑器"""
```

## 使用示例

### 示例1：语法诊断后跳转到错误行

**场景**：AI诊断发现第5行有错误

**操作步骤**：
1. AI诊断完成，显示错误信息
2. 点击"跳转到行"按钮
3. 自动切换到编辑器标签页
4. 光标定位到第5行
5. 第5行显示红色背景高亮
6. 根据AI建议修正错误

### 示例2：应用补全建议

**场景**：AI补全了缺失的单元格和表面定义

**操作步骤**：
1. AI补全完成，显示完整代码
2. 查看补全内容确认无误
3. 点击"应用建议"按钮
4. 弹出成功提示
5. 编辑器内容被替换为补全后的完整代码
6. 切换到编辑器标签页查看结果

### 示例3：应用优化建议

**场景**：AI提供了计算效率和精度优化建议

**操作步骤**：
1. AI优化完成，显示优化建议
2. 阅读优化建议内容
3. 点击"应用建议"按钮
4. 弹出确认对话框
5. 选择"是"将建议追加到编辑器
6. 编辑器末尾添加分隔线和优化建议
7. 切换到编辑器标签页查看结果

## 优势

### 1. 提高效率
- 无需手动复制粘贴
- 一键完成操作
- 减少鼠标点击次数

### 2. 减少错误
- 避免复制粘贴时的遗漏
- 确保内容完整应用
- 自动格式化和定位

### 3. 改善体验
- 直观的按钮操作
- 清晰的视觉反馈
- 智能的按钮状态管理

### 4. 灵活性
- 支持多种AI功能
- 可选择是否应用建议
- 保留原始内容（优化模式）

## 注意事项

### 1. 补全模式
- 点击"应用建议"会**替换**整个编辑器内容
- 建议先查看补全内容再应用
- 应用前可以保存当前内容作为备份

### 2. 优化模式
- 点击"应用建议"会**追加**到编辑器末尾
- 原始内容保留
- 可以选择不应用（点击"否"）

### 3. 诊断模式
- "应用建议"按钮禁用
- 使用"跳转到行"功能定位错误
- 需要手动修正错误

### 4. 分析模式
- "应用建议"按钮禁用
- 分析结果仅供参考
- 不提供直接应用功能

## 快捷操作

| 操作 | 快捷方式 |
|------|----------|
| 跳转到错误行 | 点击"跳转到行"按钮 |
| 应用补全建议 | 点击"应用建议"按钮 |
| 应用优化建议 | 点击"应用建议"按钮 → 选择"是" |
| 清除AI结果 | 点击"清除"按钮 |

## 后续改进计划

- [ ] 支持选择性应用建议（部分应用）
- [ ] 支持建议预览窗口
- [ ] 支持建议历史记录
- [ ] 支持撤销应用操作
- [ ] 支持批量应用多个建议
- [ ] 支持自定义应用方式（替换/追加/插入）

## 常见问题

### Q: 应用补全建议后，原始内容丢失了怎么办？
A: 补全模式会替换整个编辑器内容。建议在应用前：
1. 查看补全内容确认无误
2. 保存当前文件作为备份
3. 再点击"应用建议"

### Q: 优化建议追加到哪里？
A: 优化建议会追加到编辑器内容的**末尾**，并添加分隔线：
```
==================================================
AI优化建议
==================================================

[优化建议内容]
```

### Q: 如何不应用优化建议？
A: 点击"应用建议"按钮后，在确认对话框中选择"否"，建议仅在AI分析结果中显示，不会追加到编辑器。

### Q: 跳转到行后如何查看其他错误？
A: 跳转到行功能会跳转到**第一个错误**。查看其他错误的方法：
1. 在AI分析结果中查看其他错误的行号
2. 在编辑器中手动滚动到对应行
3. 重复点击"跳转到行"按钮会再次跳转到第一个错误

### Q: 按钮为什么是灰色的？
A: 按钮的启用状态取决于当前AI功能：
- "跳转到行"：有错误时启用
- "应用建议"：补全/优化模式时启用

### Q: 如何清除错误标记？
A: 重新运行"语法诊断"会自动清除之前的标记。或者修改文件内容后标记会自动更新。

## 技术支持

如有问题或建议，欢迎反馈！
